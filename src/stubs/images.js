/**
 * A simple stub you can use for when you are requiring images on your scripts (probably
 * using Webpack).
 * If you set the `addStubs` option to `true` when you instantiate the Jest-Ex runner, it
 * will automatically set it as the stub for all your images.
 */
export default {
    path: '/charito.png',
    uri: 'icon',
    width: 100,
    height: 100,
};
