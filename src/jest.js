/**
 * The reason for this file is so I can test the runner class. In order to validate its
 * main function, I would need a mock of Jest, but if I mock it, the tests wouldn't work.
 * @ignore
 */

import { runCLI } from '@jest/core';

export default { runCLI };
